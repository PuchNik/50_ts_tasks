// const names: string[] = ['bob', 'karl', 'petya', 'ilya', 'viktoriya']
//
// const printNames = (array: string[]): string => {
//     return `<ul>${array.map(name => `<li>${name.charAt(0).toUpperCase() + name.slice(1)}</li>>`)}</ul>`
// }
//
// console.log(printNames(names))